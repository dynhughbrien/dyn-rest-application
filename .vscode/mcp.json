{
  "servers": {
    "npx-dynatrace-mcp-server": {
      "command": "bash",
      "cwd": "${workspaceFolder}",
      "args": ["-c", "set -a && source .env && set +a && npx -y @dynatrace-oss/dynatrace-mcp-server@latest"]
    }
  }
}
